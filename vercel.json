{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "pnpm build && rm dist && mkdir -p dist/projects && mv .output/public/* dist/projects/",
  "outputDirectory": "dist",
  "redirects": [
    {
      "source": "/",
      "destination": "/projects/"
    }
  ]
}
